{
  "name": "image-optimize",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "preinstall": "rm -rf node_modules/sharp && npm cache clean --force",
    "postinstall": "ls -lha",
    "build": "echo THE BUILD",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "debug": "^4.1.1",
    "request": "^2.88.0",
    "sharp": "^0.24.0"
  }
}
